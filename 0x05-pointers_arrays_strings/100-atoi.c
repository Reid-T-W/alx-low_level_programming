#include "main.h"
#include <stdlib.h>

/**
 *_atoi - convert strubg to integer
 *@s - string to be converted
 *
 *Return: Always 0
 */
int _atoi(char *s)
{
	int conv;

	conv = atoi(s);
	return (conv);
}
